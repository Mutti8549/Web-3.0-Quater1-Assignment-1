let Friend_name = ["Mutti", "Talha", "Irfan", "Ali hamza"];

// running a loop on array and geeting each name from array
for (let i = 0; i < Friend_name.length; i++) {
    console.log(`Greetings: ${Friend_name[i]}`);
}